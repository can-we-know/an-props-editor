---
nav:
  title: Setter
  order: 1
---

# EventsSetter

```jsx
import { EventsSetter } from '@an/props-editor';

const store = [{ name: 'onChange' }];
const definition = [{ name: 'nativeEvents', index: 0 }];

export default () => <EventsSetter store={store} definition={definition} />;
```
